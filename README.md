# 草莓病蟲害辨識系統 - 前端介面 (Vue.js)

[![Vue.js](https://img.shields.io/badge/Vue.js-3.x-brightgreen?logo=vue.js)](https://vuejs.org/)
[![Vite](https://img.shields.io/badge/Vite-%E2%9A%A1%EF%B8%8F-blue?logo=vite)](https://vitejs.dev/)
[![Bootstrap](https://img.shields.io/badge/Bootstrap-5.x-purple?logo=bootstrap)](https://getbootstrap.com/)
[![Axios](https://img.shields.io/badge/Axios-Networking-lightgrey?logo=axios)](https://axios-http.com/)

🚀 **[點此查看部署網站 (GitHub Pages)](https://nick921003.github.io/Strawberry-detect-frontend/)**  

這是草莓病蟲害辨識系統的前端使用者介面，使用 **Vue.js 3** 和 **Vite** 建構。  
它與後端的 **Django RESTful API** 互動，提供圖片上傳、結果展示、歷史紀錄查詢等功能。  

🔗 **後端專案連結**： [StrawberryDetect_Web (Django Backend)](https://github.com/Nick921003/StrawberryDetect_Web)

---

## ✨ 主要功能

### 🖼️ 圖片上傳
- 支援點擊選擇或拖曳上傳單張或多張圖片（JPG, PNG）。
- 顯示上傳進度狀態。
- 成功上傳後自動切換至結果視圖。

### 🔍 辨識結果展示
- 使用 `ResultCard` 清晰展示原始圖片與標註後圖片。
- 顯示辨識出的物件類別、信心度（帶顏色徽章）。
- 顯示綜合嚴重程度評分與文字描述。
- 提供「繼續上傳」按鈕返回上傳介面。

### 🕒 歷史紀錄
- 區分「手動上傳」與「批次任務」歷史紀錄。
- 列表展示歷史紀錄摘要（ID、時間、狀態/嚴重程度）。
- 支援分頁瀏覽所有歷史紀錄。
- 點擊列表項可查看單筆或批次的完整詳情。

### 📋 詳細視圖
- **單筆紀錄詳情頁**：重用 `ResultCard` 顯示完整資訊。
- **批次任務詳情頁**：顯示批次摘要，並列表展示該批次包含的所有紀錄 (`ResultCard`)。

### ☁️ S3 批次觸發
- 提供表單介面，可手動輸入 S3 儲存桶與資料夾路徑。
- 觸發後端批次處理任務並顯示任務提交狀態與 Celery Task ID。
- 提供快捷連結至批次歷史頁面。

### 🎨 使用者體驗優化
- 全域統一的視覺風格（基於 **Bootstrap 5**）。
- 頁面之間平滑的淡入淡出轉場動畫。

---

## 🛠️ 技術棧

| 類別 | 技術 |
|------|------|
| 框架 | Vue.js 3 (Composition API) |
| 建構工具 | Vite |
| 路由 | Vue Router |
| HTTP 客戶端 | Axios |
| UI / 樣式 | Bootstrap 5 |

---

## 🌐 部署與自動化 (CI/CD)

本專案已設定自動化部署流程，實現完整的 CI/CD：

- **託管平台**：GitHub Pages  
- **CI/CD 工具**：GitHub Actions  
- **設定檔**：`.github/workflows/main.yml`

### ⚙️ 觸發條件
當程式碼被推送（push）到 `main` 分支時自動觸發。

### 🔁 自動化流程
1. **Checkout**：抓取最新程式碼  
2. **Setup Node.js**：設定 Node.js 環境  
3. **Install & Build**：
   - 進入 `StrawberryDetectfront/` 目錄
   - 執行：
     ```bash
     npm install
     npm run build
     ```
4. **注入密鑰**：
   - 在建置階段，GitHub Actions 會自動將儲存在 Secret 中的 `VITE_API_BASE_URL`（指向生產環境 `https://api.strawberrydetect.com/api/process`）注入到應用程式中。
5. **Deploy**：
   - 將最終的 `dist` 目錄產物自動部署到 `gh-pages` 分支，完成網站更新。

---

## 🚀 本地開發設定

### 🔧 環境需求
- Node.js（建議 LTS 版本）  
- npm（通常隨 Node.js 安裝）

### 📋 步驟

1. **Clone 儲存庫**
   ```bash
   git clone https://github.com/Nick921003/Strawberry-detect-frontend.git
   ```

2. **進入專案目錄**
   ```bash
   cd Strawberry-detect-frontend/StrawberryDetectfront
   ```
   ⚠️ *注意：所有開發指令皆需在此 `StrawberryDetectfront` 子目錄中執行*

3. **安裝依賴**
   ```bash
   npm install
   ```

4. **設定環境變數**
   在 `StrawberryDetectfront/` 目錄建立一個 `.env.local` 檔案，內容如下：
   ```bash
   VITE_API_BASE_URL=http://127.0.0.1:8000/api/process/
   ```
   （請確保後端 Django 服務運行在 `http://127.0.0.1:8000`）

5. **啟動開發伺服器**
   ```bash
   npm run dev
   ```
   應用程式將運行於 [http://127.0.0.1:5173](http://127.0.0.1:5173)（或 Vite 指定的其他端口）。

---

## 📦 可用腳本

| 指令 | 說明 |
|------|------|
| `npm run dev` | 以開發模式啟動應用程式，支援熱重載 |
| `npm run build` | 將應用程式打包成用於生產環境的靜態檔案（輸出至 `dist/` 資料夾） |
| `npm run preview` | 在本地預覽生產環境打包後的結果 |
| `npm run lint` | 執行 ESLint 檢查程式碼風格（需先設定） |

---
